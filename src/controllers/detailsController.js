const detailsController = {}

/**
 *
 * @param {*} req express request
 * @param {*} res express response
 */
detailsController.getImagesByCategory = async (req, res) => {
	try {
	} catch (error) {}
}
/**
 *
 * @param {*} req express request
 * @param {*} res express response
 */
detailsController.getImagesDetailsById = async (req, res) => {
	try {
	} catch (error) {}
}

module.exports = detailsController
